{
  "name": "@relay/link-web",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs && npm run build",
    "build": "wrangler deploy --dry-run --outdir /tmp/relay-web-build"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
